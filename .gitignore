__pycache__/
*.pyc

# Generated/runtime data — rebuilt by refresh_rm_costs.py / server.py, not source
costing_history.db
server.log
server_run.log
server_run.err.log
server_err.log
refresh_output.log

# Flask session-signing key — generated on first run (server.py), never
# checked in (anyone with it could forge session cookies).
.flask_secret_key

# One-off analysis/debug output from data extraction sessions
*_dump.txt
cirrus_new_products_extract.json
extract_cirrus_sheets.py

# Source data — real Ramco/Item Master exports, kept locally only (some
# individual files exceed GitHub's 100MB push limit). Code reads these at
# runtime; they are not meant to be version-controlled.
Accounts W - 27.07.2026/
Item Master/
*.xlsx
*.xls
